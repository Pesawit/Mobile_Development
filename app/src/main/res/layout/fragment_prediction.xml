<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="@color/white">

    <!-- ImageView for the predicted image -->
    <ImageView
        android:id="@+id/iv_predicted_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@color/white3"
        android:scaleType="centerCrop" />

    <!-- TextView for the prediction result -->
    <TextView
        android:id="@+id/tv_predicted_result"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@color/Mustard"
        android:text="Hasil Prediksi"
        android:textColor="@color/black"
        android:textSize="18sp" />

    <!-- TextView for the recommendation result -->
    <TextView
        android:id="@+id/tvRecommendation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Rekomendasi: "
        android:textColor="@color/black"
        android:textSize="16sp"
        android:layout_marginTop="16dp"
        android:background="@color/Mustard" />

    <!-- ProgressBar to show loading state -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" /> <!-- Initially hidden -->
</LinearLayout>
